apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  # Create the XR/Claim
  - command: kubectl apply -f ../../../examples/claim_all.yaml
  # Wait for Storage Account to become fully available before proceeding
  - command: kubectl wait --for condition=Ready --timeout=180s xtstorageaccount.crossplane.fnietoga.me/storage-account-all